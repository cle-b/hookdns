language: python
python: "3.6"
sudo: required
before_install:
  - cat /etc/hosts
env:
- TOX_ENV=py36
install:
- pip install tox
script: tox -e $TOX_ENV
after_success:
- pip install coveralls
- coveralls
